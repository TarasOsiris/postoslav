node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# App data (root-anchored so these only ignore the runtime dirs at the project
# root — an unanchored `media/` also matched `src/routes/tiktok/media/` and
# silently excluded the media route's source file from git).
/data/
/media/
dev.db*
